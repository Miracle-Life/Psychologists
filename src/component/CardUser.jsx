import React, {useEffect, useState} from "react";import PropTypes from "prop-types";function CardUser({removeFavorites, addToFavorites, details, deleteUser, index}) {    const [willFavorite, setFavorite] = useState(false);    // constructor(props) {    //     super(props);    //     this.state = {    //         willFavorite: false    //     }    // }    // const {index, deleteUser, addToFavorites, removeFavorites} = this.props;    // const {type, photo, key, email} = this.props.details    useEffect(()=>{        console.log();    })    return (        <>            {Object.keys(details).map(key => {                const {email, photo, type} = details[key]                return (                    <div className="card " style={{width: "18rem"}} key={key}>                        <img                            className="card-img-top"                            src={photo === "null" ? 'https://hsto.org/webt/-j/go/fx/-jgofxkmtexlfds_uh_c4eklqgu.jpeg' : photo}                            style={{width: "16,5rem", height: "13rem"}}                            alt=""                        />                        <div className="card-body">                            <h6 className="card-title">{key}</h6>                            <h6 className="card-title">                                {email}                            </h6>                            <h6 className="card-title">                                {type}                            </h6>                        </div>                        <div className="card-body">                            <div className="d-flex justify-content-between ">                                <button                                    type="button"                                    // className="btn btn-success"                                    className={willFavorite ? "btn btn-success" : "btn btn-secondary"}                                    onClick={() => {                                        setFavorite({                                            willFavorite: !willFavorite                                        })                                        willFavorite ? removeFavorites(details.key) : addToFavorites(details.key)                                    }}                                >                                    Liked                                </button>                                <button                                    className="btn btn-outline-danger"                                    onClick={deleteUser}                                >Delete User                                </button>                            </div>                        </div>                    </div>                )            })}        </>    )}CardUser.propTypes = {    removeFavorites: PropTypes.func,    addToFavorites: PropTypes.func,    deleteUser: PropTypes.func,    details: PropTypes.shape({        type: PropTypes.string,        photo: PropTypes.string,        email: PropTypes.string,    }),    index: PropTypes.string,}export default CardUser